import React from 'react';
import {SpinContainer, Shape, Image, Box } from './images';

export const ProfileImage = () => (
    <Box>
        <SpinContainer>
            <Shape>
                <Image/>
            </Shape>
        </SpinContainer>
    </Box>
);